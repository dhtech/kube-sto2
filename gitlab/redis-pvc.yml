kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    k8s-app: gitlab
  name: gitlab-redis-data
  namespace: gitlab
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
