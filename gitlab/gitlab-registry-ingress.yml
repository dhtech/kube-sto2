apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    labels:
        k8s-app: gitlab
    name: gitlab-registry-ingress
    namespace: gitlab
    annotations:
        kubernetes.io/tls-acme: "true"
spec:
    tls:
    - secretName: gitlab-registry-ingress-tls
      hosts:
      - registry.tech.dreamhack.se
    rules:
    - host: registry.tech.dreamhack.se
      http:
        paths:
        - path: /
          backend:
            serviceName: gitlab
            servicePort: 80

