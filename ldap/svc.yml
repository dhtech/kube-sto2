apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: ldap
  name: ldap
  namespace: ldap
spec:
  externalIPs:
  - 77.80.255.101
  ports:
  - name: ldap
    port: 636
    protocol: TCP
    targetPort: 636
  selector:
    k8s-app: ldap
  sessionAffinity: None
  type: ClusterIP
